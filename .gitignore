# Database
data/
*.db
*.db-shm
*.db-wal

# Compiled binaries
bin/
/migrate
/downsample
/server
/backfill
/import
*.exe

# Claude Code
.claude/

# Environment files
.env
.env.*
!.env.example
docker-compose.override.yml

# macOS
.DS_Store
export.zip
pass

# Health Auto Export automation configs (contain API keys in headers)
hae_*.json

# Recovered-from-broken local git state, preserved on disk so it can
# be inspected / rsynced if a future op needs the old reflog.
.git.broken/
